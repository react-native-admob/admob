import FullScreenAd from './FullScreenAd.mdx';

Creates [Rewarded Ad](https://developers.google.com/admob/android/rewarded) and register event listeners to various events.

:::tip

If you are going to use Rewarded Ad inside _functional component_, consider using [`useRewardedAd`](/docs/api/hooks/useRewardedAd).

:::

## Useage Example

TODO

<FullScreenAd type="Rewarded" />
